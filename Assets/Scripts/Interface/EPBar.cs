using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class EPBar : ProgressBar {

	/*UnitParams playerParams;

	void Start () {
		playerParams = GameManager.player.GetComponent<UnitParams> ();
	}

	void Update ()
	{
		if (playerParams.EP != currentPercent) {
			isOn = true;
			if (playerParams.EP < currentPercent && Mathf.Abs (playerParams.EP - currentPercent) > 3f) {
				currentPercent -= speed * Time.deltaTime;
			} else if (playerParams.EP > currentPercent && Mathf.Abs (playerParams.EP - currentPercent) > 3f) {
				currentPercent += speed * Time.deltaTime;
			}
		} else {
			isOn = false;
		}
		loadingBar.GetComponent<Image> ().fillAmount = currentPercent / 100;
	}*/
}
